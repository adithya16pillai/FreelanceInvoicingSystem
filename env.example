# MongoDB Connection String
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/freelance-invoicing

# Example for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/freelance-invoicing?retryWrites=true&w=majority

# Optional: JWT Secret for authentication (if you add auth later)
# JWT_SECRET=your-secret-key-here 